'use strict';

const tabset = new Tabset('myTabset');

// console.log(mainDiv.childNodes);

/*
Array.prototype.forEach.call(mainDiv.children, childDiv => {
    console.log();
    console.log(childDiv.children[0]);
    console.log(childDiv.children[1]);
});
*/

// const tab = new Tabset(document.getElementById('myTabset'));

/*
document
    .getElementById('btn')
    .addEventListener('click', () => tab.showIndex(2));
*/

// console.log(tab);